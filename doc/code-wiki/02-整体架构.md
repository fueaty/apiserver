# 02 整体架构

## 1. 分层架构图

```mermaid
flowchart TB
    CRON["触发层：生产 3 条 cron（唯一调度）<br/>08:00 / 21:00 采集 · 02:00 清理"]
    HTTP["触发层：HTTP 请求（FastAPI 形态）"]
    CELERY["触发层：Celery 任务（容器形态）"]

    subgraph ORCH["编排层 script/"]
        ET["everday_task.py<br/>顺序编排 + 企微通知"]
        CP["collection_pipeline.py<br/>采集→mock过滤→容量预检→飞书写入"]
        EX["export_today_headlines.py<br/>当日快照导出"]
    end

    subgraph BIZ["业务层 app/services/"]
        COL["collection<br/>采集引擎"]
        FEI["feishu<br/>读写底座"]
        SEL["selection<br/>规则/ML 选材"]
        PUB["publication<br/>6 平台发布"]
        ANA["analysis<br/>LLM 特征/insights"]
        CF["content_fetcher<br/>正文子管道"]
    end

    subgraph IFACE["接口层（仅常驻形态生效）"]
        MW["app/middleware × 4"]
        EP["app/api/v1 端点 × 10"]
    end

    subgraph SUPPORT["支撑层"]
        CFG["core/config<br/>Settings + ConfigManager 热重载"]
        TASKS["app/tasks（Celery）"]
        UTILS["app/utils + app/wework"]
        YML["config/*.yaml"]
    end

    EXT["外部系统：飞书多维表格 · 9 采集站点 · 6 发布平台 · 企业微信 · LLM API · Redis"]

    CRON --> ET
    ET --> CP
    ET --> EX
    HTTP --> MW
    MW --> EP
    CELERY --> TASKS
    EP --> COL
    COL --> FEI
    FEI --> SEL
    SEL --> PUB
    PUB --> ANA
    COL -.-> CF
    BIZ --> EXT
    SUPPORT -.-> BIZ
```

## 2. 核心数据流

```mermaid
flowchart TB
    subgraph MAIN["主链路：采集 → 入库"]
        S1["9 站点 collect()<br/>RSS / API / Playwright"] --> S2["CollectionEngine 归一化<br/>feishu 字段体 + hotspot_enrich"]
        S2 --> S3{"split_write_set<br/>mock 过滤"}
        S3 -->|真实记录| S4["ensure_capacity 容量预检<br/>不足则最旧优先删到水位"]
        S3 -->|mock 行| S5["剔除，不入库"]
        S4 --> S6["按 title 当日 upsert<br/>batch_add_records 500/批"]
        S6 --> HL[("headlines 表")]
    end

    subgraph BODY["正文子链路"]
        B1["content 为空的记录"] --> B2["robots 检查 → HTTP 抓取 → 提取"]
        B2 --> B3["回写 content 字段<br/>fetch / backfill 端点 · 回填脚本"]
    end

    subgraph ANALY["分析子链路"]
        C1["FeishuDataLoader 按日期读取"] --> C2["LLMProcessor 并发批处理<br/>OpenAI 兼容客户端"]
        C2 --> C3["FeatureAnalyzer 特征分析<br/>HotspotClassifier 分类"]
        C3 --> AI[("ai_insights 表<br/>build_insight_fields 14 字段")]
    end

    subgraph SELPUB["选材 → 发布链路"]
        D1["SelectionEngine 五维打分<br/>或 MLSelectionEngine 预测"] --> D2[("content_selection 表")]
        D2 --> D3["PublicationManager.publish()<br/>平台工厂 → 前置插件 → publish → 后置插件"]
        D3 --> D4["6 平台 API"]
        D3 --> D5[("publish_tasks 表")]
        D5 --> D6["AnalysisService 效果分析"]
    end

    subgraph CLEAN["清理链路（cron 02:00）"]
        E1["① 按时间：删 25 天前记录"] --> E2{"仍高于 WATERMARK?"}
        E2 -->|是| E3["② 最旧优先压到水位内"]
        E2 -->|否| E4["结束"]
    end

    HL -.-> B1
    HL -.-> C1
    HL -.-> D1
```

## 3. 容量模型（本项目最重要的不变式）

### 3.1 常量（唯一来源：`app/services/feishu/limits.py`）

| 常量 | 值 | 含义 |
|---|---|---|
| `TABLE_RECORD_LIMIT` | 20,000 | 飞书单表硬上限（错误码 1254103） |
| `BATCH_WRITE_LIMIT` | 500 | 单次写接口上限（错误码 1254104） |
| `RETENTION_DAYS` | 25 | 目标保留窗口 |
| `DAILY_BUDGET` | 630 | 规划日量（**均值口径**，非上界口径） |
| `WATERMARK` | 17,000 | 容量水位：写前预检把表压到这里以下 |
| `WARNING` | 18,500 | 告警线 |
| `LIST_PAGE_SIZE` | 500 | 清理时分页读取大小 |

站点单轮上界（唯一来源：`app/services/collection/site_caps.py` `SITE_ROUND_CAPS`）：
people_daily 50 / xinhua 30 / cctv 50 / thepaper 100 / weibo 50 / baidu 50 / zhihu 50 / tech_36kr 50 / xiaohongshu 30，
**Σ(单轮) = 460**，按每天 2 轮 ⇒ 最坏日 **920** 条。

### 3.2 三条必须知道的算术事实

1. `Σ(上界) = 460 ≤ WATERMARK = 17,000` —— 单轮必然写得下。这是 **G1 守卫**（`site_caps.py:114`，import 期硬失败）。
2. `460 × 2 = 920 > DAILY_BUDGET = 630`，且 `> 17000/25 = 680` —— 「上界口径」装不进日预算；`DAILY_BUDGET` 是均值口径，不可写成硬断言。**G2 守卫**（`site_caps.py:126`）改为把最坏日上界**棘轮冻结**在 920：任何站点上限调大都会 import 期 raise。
3. `920 × 25 = 23,000 > 20,000` —— 「最坏情况下 25 天装不下」为真。实际净日增超过 680 时，写前预检会**最旧优先**压到水位（**比 25 天新的记录照删**），保留窗口随之压缩。

> 安全底线是 **G1（单轮可写）**，不是「日总账」；手动重跑不在「2 轮/天」之内。
> `limits.py` 自身还有 `_assert_capacity_budget()`（`limits.py:75`）：`RETENTION_DAYS × DAILY_BUDGET ≤ WATERMARK`、`WATERMARK < WARNING < TABLE_RECORD_LIMIT` 三条不变式，同样 import 期硬失败。
> **`limits.py` 保持零 import 是承重属性**（`test_capacity_budget.py` 靠「复制到临时目录 + 子进程 import」做变异测试），不得反向依赖任何模块。

守卫触发关系：

```mermaid
flowchart LR
    subgraph CAPS["site_caps.py（import 期执行）"]
        SRC["SITE_ROUND_CAPS<br/>9 站点 Σ = 460"] --> WCD["ROUNDS_PER_DAY = 2<br/>WORST_CASE_DAILY = 920"]
        WCD --> G1{"G1 守卫<br/>460 ≤ WATERMARK?"}
        WCD --> G2{"G2 棘轮<br/>920 ≤ 基线 920?"}
    end
    subgraph LIM["limits.py（零 import）"]
        INV{"_assert_capacity_budget<br/>25×630 ≤ 17,000<br/>17,000 < 18,500 < 20,000?"}
    end
    G1 -->|违反| RAISE["ValueError：进程起不来<br/>（故意 fail-loud，不 warn）"]
    G2 -->|违反| RAISE
    INV -->|违反| RAISE
    G1 -->|通过| OK["import 成功，常量不变式成立"]
    G2 -->|通过| OK
    INV -->|通过| OK
```

### 3.3 导入方向（单向、无环）

```mermaid
flowchart LR
    SITES["sites/*<br/>（9 站点实现）"] --> SCAP["collection.site_caps<br/>上界名录 + G1/G2 守卫"] --> LIM["feishu.limits<br/>容量常量（零 import）"]
    LIM -.->|反向 import 被禁止<br/>（变异测试的隔离前提）| SCAP
```

## 4. 模块依赖关系（import 视角）

```mermaid
flowchart TB
    MAIN["app/main.py"] --> APIR["api/v1/api.py"] --> EPS["endpoints/*（10 个）"]
    EPS --> AUTH["auth.py:verify_token<br/>（认证单点，其余 9 端点全部引用）"]
    EPS --> ENG["collection/engine.py"] --> SITES["sites/*（9 站点）"]
    SITES --> SC["site_caps.py"] --> LIM["feishu/limits.py"]
    EPS --> FSV["feishu/feishu_service.py"] --> FR["field_rules + limits"]
    EPS --> SEL["selection/engine.py<br/>selection/ml_engine.py"]
    EPS --> PM["publication/manager.py"] --> PL["platforms/*（6 平台）"]
    PM --> FSV
    EPS --> ANA["analysis/*"] --> FSV
    CFG["core/config：settings + config_manager<br/>（所有 services / endpoints 依赖）"]
    SCRIPT["script/ 编排脚本"] --> ENG
    SCRIPT --> FSV
    SCRIPT --> WW["wework/notification_push<br/>（企微告警）"]
    TASKS["app/tasks（Celery）"] --> SVS["services/* 的异步包装"]
    CFG -.-> EPS
```

关键耦合点：

- **认证单点**：`verify_token` 定义于 `app/api/v1/endpoints/auth.py:85`，其余 9 个端点模块全部 `from ...auth import verify_token`。
- **飞书写入收口**：所有写路径最终都走 `FeishuService.batch_add_records()`（`feishu_service.py:477`）——20,000 上限检查、500 分片、1254103 自愈重试都在这一处。
- **配置单点**：`config_manager`（sites/platforms/credentials/analysis 四个 yaml）与 `settings`（env/pydantic）双实例并存，见 [11-配置系统](11-配置系统.md)。

## 5. 三种运行形态

| 形态 | 入口 | 使用场景 | 说明 |
|---|---|---|---|
| **cron 批任务** | `script/run_daily_task.sh` / `script/scheduled_cleanup.sh` | **生产唯一实际执行路径** | openEuler 24.03 + Python 3.11.6，`/opt/apiserver`；无 systemd，改代码下次 cron 即生效 |
| **FastAPI 常驻** | `python -m app.main`（uvicorn） | 开发调试 / 手动触发接口 | `app/main.py`；`/docs` Swagger；依赖 Redis 做 health 检查（不可达时 status=degraded） |
| **Docker 容器** | `docker-compose.yml` | 容器化部署（与生产形态**不同**，勿混用） | 4 服务：`apiserver` / `redis` / `mongodb` / `playwright`；`deploy.sh` 做健康检查（服务名由 `test_deploy_service_names.py` 守护） |

## 6. 关键设计决策与已知缺口

| # | 决策/现状 | 细节 |
|---|---|---|
| 1 | **站点失败回退 mock，但三写入面都剔除** | `fallback_or_empty()`（`mock_utils.py:135`）默认生产返回空列表；`APISERVER_ALLOW_MOCK=1` 才产出带 `is_mock` 标记的演示数据；写入前由 `split_write_set`（`collection_pipeline.py:149`）、`build_headline_records`（`enhanced_collection.py:74`）、`split_real_and_mock`（`feishu.py:27`）过滤 |
| 2 | **写前容量预检只有一处** | `collection_pipeline.py:298` `ensure_capacity`；另两个写入面（`enhanced_collection.py:103/:272`、`POST /feishu/sync`）没有，唯一兜底是 `batch_add_records` 内部的 1254103 自愈重试（`feishu_service.py:532-546`） |
| 3 | **按 title 当日 upsert** | 只与当天记录比对，跨天重复是设计语义（用于观察热点持续天数） |
| 4 | **归档搬移不在调度内** | `manage_history_data.py` 需人工执行 |
| 5 | **重复实现** | `generate_content_id` 在 `app/utils/id_generator.py:13` 与 `secret/generate_auth_key.py:60` 各一份 |
| 6 | **站点 0 产出不判因** | 有站点级缺口告警（`collection_pipeline.py:113-120` 推企微），但无法区分「无新内容」与「静默失败」 |
| 7 | **死代码** | `app/api/v1/__init__.py:15` 的 `enhanced-collection` 前缀未被挂载（真实前缀是 `/enhanced`） |
| 8 | **硬编码路径** | `zhihu.py:43` 读 `/root/apiserver/config/zhihu.yaml`（绝对路径，生产靠软链兼容） |

## 7. 各模块详文档

采集 [04](04-采集模块.md) ｜ 飞书 [05](05-飞书模块.md) ｜ 选材 [06](06-选材模块.md) ｜ 发布 [07](07-发布模块.md) ｜ 分析 [08](08-分析模块.md)
